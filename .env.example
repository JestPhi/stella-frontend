# Environment Variables Template
# Copy this to .env.local for development or set in production environment

# =============================================================================
# 🔒 SERVER-SIDE ONLY (NEVER expose these to client-side)
# =============================================================================

# Backend API URL - used by Next.js API routes to communicate with your backend
# This should NOT have NEXT_PUBLIC_ prefix to keep it server-side only
STELLA_APP_HOST=https://your-backend-api.com

# Firebase Admin SDK (for server-side token validation) - KEEP SECRET
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYourPrivateKeyHere\n-----END PRIVATE KEY-----"

# Database credentials (if using)
# DATABASE_URL=postgresql://user:pass@host:port/db

# =============================================================================
# 🌐 CLIENT-SIDE SAFE (these are meant to be public)
# =============================================================================

# Firebase Client Configuration - these are SAFE to expose
# Firebase designed these to be public (they identify your project)
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id

# Public CDN URLs for static assets
NEXT_PUBLIC_STORJ_PUBLIC_URL=https://link.storjshare.io/your-bucket
NEXT_PUBLIC_STELLA_REACT_NATIVE_FOR_WEB_HOST=https://your-app-domain.com

# =============================================================================
# 🐳 Docker & Production Settings
# =============================================================================

NODE_ENV=production
PORT=3000

# =============================================================================
# 📝 NOTES
# =============================================================================

# ✅ SAFE to expose (NEXT_PUBLIC_*):
#   - Firebase client config (they're meant to be public)
#   - Public CDN URLs
#   - App domains for postMessage

# ❌ NEVER expose (no NEXT_PUBLIC_ prefix):
#   - API endpoints/backend URLs
#   - Database credentials  
#   - Private keys
#   - Secrets/tokens
#   - Firebase Admin SDK credentials

# 🔒 Security reminder:
#   - Use Firebase security rules to protect data
#   - Validate all API requests server-side
#   - Never trust client-side data
